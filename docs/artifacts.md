<img src="../resources/brand/cat.png" alt="" width="72" align="right">

# 1С: Артефакты

> <img src="../resources/brand/cat.png" alt="" width="26" align="left"> Кнопка «Разобрать» появляется прямо в редакторе бинарного файла.

Панель **1С: Артефакты** в Проводнике показывает поддерживаемые файлы проекта: конфигурации (`*.cf`), расширения (`*.cfe`), внешние обработки (`*.epf`), отчёты (`*.erf`) и соответствующие каталоги исходников. Сценарии Vanessa (`*.feature`) живут в [панели тестирования](testing.md), не здесь. Проекты EDT стоят в тех же разделах с пометкой `EDT`.

![Панель «1С: Артефакты»](../walkthrough/images/step4.png)

## Действия

- Из дерева: **Открыть**, **Собрать**, **Разобрать**.  В контекстном меню — те же команды и **Удалить**.
- При открытии бинарного файла (`.cf`, `.cfe`, `.epf`, `.erf`) в редакторе доступна кнопка **Разобрать**.

Сборка и разбор выполняются командами vanessa-runner: собранный файл предлагается в каталоге сборки, разобранный исходный код в каталоге объекта. Куда именно, спрашивается перед запуском.

## Настройки

`1c-platform-tools.artifacts.exclude` — сегменты пути, исключаемые при сканировании артефактов (например `oscript_modules`, `build`).
