<img src="../resources/brand/cat.png" alt="" width="72" align="right">

# 1С: Артефакты

> <img src="../resources/brand/cat.png" alt="" width="26" align="left"> Кнопка «Разобрать» появляется прямо в редакторе бинарного файла.

Панель **1С: Артефакты** в Проводнике показывает поддерживаемые файлы проекта: конфигурации (`*.cf`), расширения (`*.cfe`), внешние обработки (`*.epf`), отчёты (`*.erf`) и соответствующие каталоги исходников. Сценарии Vanessa (`*.feature`) живут в [панели тестирования](testing.md), не здесь.

![Панель «1С: Артефакты»](../walkthrough/images/step4.png)

## Действия

- Из дерева: **Открыть**, **Собрать**, **Разобрать**. Запуск тестов со статусами и переходом к падению (в том числе сценариев Vanessa из .feature) — в [панели тестирования](testing.md). В контекстном меню — те же команды и **Удалить**.
- При открытии бинарного файла (`.cf`, `.cfe`, `.epf`, `.erf`) в редакторе доступна кнопка **Разобрать**.

Сборка и разбор выполняются командами vanessa-runner по тем же путям, что и команды дерева **1С: Инструменты** (`1c-platform-tools.path.*`).

Каталог по умолчанию подставляется по области артефакта: для тестовых расширений (исходники в `tests/cfe`, собранные `*.cfe` в `build/out/tests/cfe`) это их каталоги, а не каталоги расширений решения. Имя расширения тоже берётся из своих исходников — каталог `tests/cfe/yaxunit-test` с расширением `Тесты` разбирается под своим именем.

## Настройки

`1c-platform-tools.artifacts.exclude` — сегменты пути, исключаемые при сканировании артефактов (например `oscript_modules`, `build`).
