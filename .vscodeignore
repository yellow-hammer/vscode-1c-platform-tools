# Исходный код TypeScript (компилируется в out/)
src/**
**/*.ts
**/*.tsx

# Конфигурация разработки
.vscode/**
.vscode-test/**
.gitignore
.yarnrc
.spellignore
vsc-extension-quickstart.md
**/tsconfig.json
**/eslint.config.mjs
**/.vscode-test.*
**/.secretlintrc.json
**/.cursorignore
**/.cursor

# Тесты
**/*.test.js
**/*.test.js.map
**/test/**
**/fixtures/**

# Source maps
**/*.map

# Сборка и зависимости
**/node_modules/**
**/esbuild.config.mjs
build/**
**/build/**
scripts/**

# onec-debug-adapter (DAP) качается в рантайме с GitHub Releases, в VSIX не бандлится
_onec-debug-adapter/**
bin/onec-debug-adapter/**

# Временные файлы
*.log
*.tmp
*.cache
.DS_Store
Thumbs.db

# CI/CD
.github/DISCUSSION_TEMPLATE/**
.github/ISSUE_TEMPLATE/**
.github/workflows/**
.github/FUNDING.yml
.github/PULL_REQUEST_TEMPLATE.md

# Документация: ссылки README при упаковке переписываются vsce на GitHub-URL,
# поэтому docs в VSIX не нужен (walkthrough при этом остаётся в пакете)
docs/**
CONTRIBUTING.md
cliff.toml

# Package manager
package-lock.json.bak
yarn.lock
pnpm-lock.yaml
